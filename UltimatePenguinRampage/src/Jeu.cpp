#include "Jeu.h"

// Construceur
Jeu::Jeu(SDL_Window* &fenetre, SDL_Surface* &ecran) {

    Start(fenetre, ecran);
}

// Destructeur
Jeu::~Jeu() {
}

// Gère tout le jeu
int Jeu::Start(SDL_Window* &fenetre, SDL_Surface* &ecran) {
    bool quit = false;

    while(!quit) {

    }

    return 0;
}
